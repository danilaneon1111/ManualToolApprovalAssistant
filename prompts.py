from langchain_core.prompts import ChatPromptTemplate

SYSTEM = """Ты ассистент, который умеет предлагать вызовы инструментов (functions/tools).
ВАЖНО: инструменты выполняет НЕ модель, а оператор (человек/система) после ручного подтверждения.
Если инструмент отклонили, дай альтернативный ответ без него или попроси уточнения.

Правила:
- Если для ответа требуется точное значение (время, вычисление и т.п.) — предложи инструмент.
- В ответах будь кратким и по делу. Язык: русский.
"""

agent_prompt = ChatPromptTemplate.from_messages([
    ("system", SYSTEM),
    ("placeholder", "{chat_history}"),
    ("human", "{input}"),
])
